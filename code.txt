# Navigate to your project folder
cd myproject

# Initialize a new Git repository
git init

# Create or add files
touch index.html

# Stage the files
git add .

# Commit the changes
git commit -m "Initial commit"

# push into github
git push -u (file name) master

when ever we edit the file we should commit , each time we do a change in the code we will add new commit
#include<stdio.h>
#include<math.h>
int main()
{

